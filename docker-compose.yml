version: "3.8"

services:
  editos-api:
    build: .
    ports: ["8000:8000"]
    volumes: ["editos_data:/data"]
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: unless-stopped

volumes:
  editos_data:
